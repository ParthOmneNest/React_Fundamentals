Every technical stack/programming lang. has a built-tool
Maven - Web Application
Gradle- App Application

svg-simple vector graphics used for resizable image sizing

public folder:- visible to output artifact
src-assets:- not visible to end-user

Strict mode:- used to embed something inside in this (e.g. App)

Fragments <></> is better for production-based deployments.
Used for wrapping, streaming services used this.
If don't want extra layer then use this

When going from one page to another page it's not only about re-direction,
State management is also required.
e.g When going from one banking page to another our userId needs to be preserved in order to show details of us regarding that page

Routes- a whole package of links
Route- a link inside the whole package of links(Routes)

- Always keep the routes inside the App.jsx and the source & destination files should be different
- - Source files will contain the useNavigate() and navigate where the destination file is present.

- Say for e-commerce sites once cart is filled and we refreshed the browser the cart count is still their because of states

- State Management :- managing app states(var,const,api)->preserve (local storage in memory)

- Jitna jyada data utna jyada rendering rendering is one of the issue

- more the frequency of data more the no. of fields will be generated so here SQL is not suitable for handling hot data that's why NoSQL is used. That's why Swiggy used NoSQL microsoft's Fabric and uses KQL(Kusto Query Language)

- Assignment 1
- integrate google authentications from google docs
- do regex pattern for email
- Create a card containing card with dummy data